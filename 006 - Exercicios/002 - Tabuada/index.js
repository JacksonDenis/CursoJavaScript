const MULTIPLICADOR = 7;

for (let multiplicado = 0; multiplicado <= 100; multiplicado++) {
    let resultado;
    resultado = MULTIPLICADOR * multiplicado;
    console.log(multiplicado + " x " + MULTIPLICADOR + " = " + resultado);
    
}